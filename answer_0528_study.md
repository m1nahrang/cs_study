## 1. 소거코드에 대해 설명해주세요.
- 데이터 신뢰성을 높일 수 있는 방법
- 데이터를 k개의 데이터 블록과 m개의 패리티 블록으로 분할 및 부가하여 저장하는 방식
- 최대 m개의 블록이 손상돼도 남은 k개의 블록으로 원본을 재구성할 수 있음
- 이를 통해 3-copy 방식에 비해 훨씬 적은 추가 공간으로 11 nines 내구성 제공 가능
- 단, 추가적인 패리티 블록을 관리해야 하므로, 쓰기 및 읽기 레이턴시 증가
- 저장 용량이 지배적인 대용량, 콜드 데이터에 적합

## 2. DynamoDB에서 제공하는 global secondary index에 대해 설명해주세요.
- global secondary index는 별도의 partition key와 sort key의 조합으로 만든 index를 의미
- 특정 접근 패턴을 높은 성능으로 처리하기 위함
- 파티션 수가 늘수록 핫 파티션은 줄지만, 조회 시 다중 파티션 병합으로 인해 복잡도 증가

## 3. Multipart 업로드 과정에 대해 설명해주세요.
- 대용량 객체를 여러개의 작은 파트로 쪼갠 후, 이를 별도로 업로드하는 과정
- 업로드 과정
  1. 클라이언트에서 스토리지에 초기화 요청을 하여 uploadID를 발급받음
  2. 클라이언트가 대용량 객체를 쪼갠 후, 각 파트를 uploadID와 함께 전송 -> 스토리지는 응답으로 체크섬이 들어있는 ETag 반환
  3. 모든 업로드가 끝나면 uploadID, 파트 번호, ETag들을 전송 -> 스토리지는 파트 번호를 재조립 후 최종 객체를 만들고 성공 응답
  4. 스토리지는 파트 원본이나 중단된 파트는 GC로 제거

## 4. Redis의 정렬 집합 자료형이 순위표 구현에 유리한 이유를 설명해주세요.
- 정렬 집합은 내부적으로 해시 테이블과 skip list로 구현됨
- 해시 테이블은 유저->점수 매핑을 담당하며, skip list는 점수->유저 매핑을 담당
  - 이때 skip list는 삽입/수정/검색이 O(log n) 보장
- 이 구조로 점수 증가, 상위 N명 조회, 특정 유저 순위 등을 모두 O(log n)으로 처리 가능
