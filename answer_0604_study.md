## 외부 결제 페이지의 작동 방식을 설명해주세요
1. 클라이언트가 결제 버튼을 누르면 Payment Service에 주문 정보 전달
2. Payment Service가 PSP에 nonce를 포함한 결제 정보를 담아 결제 등록 요청을 보냄
3. PSP는 주문 식별을 위한 token을 발급
4. Payment Service는 토큰을 자체 DB에 저장하고, PSP 호스팅 결제 페이지를 띄움
5. 결제 과정이 끝나면 PSP 프론트가 redirect URL로 브라우저를 이동시키면서, webhook을 통해 결제 결과를 Payment Service에 공유
6. 정산/조정 과정을 통해 데이터의 누락, 중복을 처리

## 보상기반분산트랜잭션에 대해 설명해주세요
- TC/C(Try-Confirm/Cancel)가 대표적인 보상기반 분산트랜잭션
- Try 단계 이후 Confirm이나 Cancel 단계로 넘어가는 구조
- Try 단계에서 성공했다면 Confirm으로, 실패했다면 Cancel로 넘어감
- 2PC와 다르게 각 Phase를 독립적인 트랜잭션으로 처리하여 장시간 락이 없다는 장점이 있음
- 보상 로직을 비즈니스 계층에서 구현해야 함으로 코드 복잡도가 증가하는 단점이 존재

## 멱등성(Idempotency)은 왜 필요하며 실무에서 어떻게 구현할지 설명해보자
- 네트워크 재시도나 사용자의 더블 클릭 등으로 인해 같은 결제 요청이 서버로 두번 이상 들어올 수 있다
- 이때 exact-once를 보장해야하는데 이를 멱등성이라고 한다
- 구현하는 패턴은 클라이언트가 Idempotency-key를 생성하여 HTTP 헤더에 실어 보내는 방법, DB 테이블의 unique 제약조건으로 처리하는 방법 등이 있다

## 재현성(Reproducibility)이 디지털 월렛에 중요한 이유와 이벤트 소싱(Event Sourcing)이 이를 어떻게 돕는지 설명해보자
- 재현성은 회계 감사/분쟁 처리 시 과거 특정 시점의 지갑 잔액과 변화 원인 등을 재현하여 설명하는 것을 의미
- Event Sourcing은 명령 검증 후 이벤트 목록에 기록하면, 이후에 이벤트를 순서대로 재생하여 그 당시의 상태를 재현할 수 있음
- 이벤트 로그가 Single Source Of Truth를 성립시켜줌
- 이를 통해 디지털 월렛같은 신뢰성/불변성이 중요한 서비스에서 이를 제공
